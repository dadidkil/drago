# Журнал выполненных действий

Формат: дата (МСК) — кто — что сделано — результат. Добавляйте записи при каждом изменении на сервере.

## 2026-09-24 — Claude Code (облачная среда разработки)

| # | Действие | Результат |
|---|---|---|
| 1 | Проверка доступа к серверу `root@2.56.90.240` из среды разработки | ❌ TCP 22 недоступен: политика сети облачной среды разрешает только HTTPS через прокси. **На сервере ничего не менялось.** |
| 2 | Проверка доступа к https://vk.ru/top_drago и сайтам МГПУ/МосРСО | ❌ заблокировано политикой среды; исследование проведено через веб-поиск (см. `research-drago.md`) |
| 3 | Проектирование и разработка монорепозитория: web (сайт, кабинет, админка), telegram-bot, vk-bot, worker, packages/database, shared, core | ✅ |
| 4 | Схема БД (Prisma 7, PostgreSQL 16/17), миграция `init`, сид (роли и права, настройки, проверенный контент) | ✅ миграция применена на локальной тестовой БД |
| 5 | Проверки: typecheck всех пакетов; 28 юнит-тестов (RBAC, валидация, криптография, rate limit, XSS-санитайзер) | ✅ |
| 6 | E2E в браузере (Playwright, dev-сервер): приглашение суперадмина → пароль → обязательная 2FA → вход с TOTP → создание пользователя, объявления, мероприятия, задачи, новости → заявка с сайта → обход 35 страниц кабинета и админки → вход бойца и проверка отказа в доступе к админке (403) и к чужому файлу (404) | ✅ ошибок нет |
| 7 | Production-сборка Next.js (standalone): строгий CSP с nonce, все скрипты с nonce, гидратация и server actions работают | ✅ |
| 8 | Сборка всех Docker-образов (web, worker, telegram-bot, vk-bot, migrate) и запуск полного стека `docker compose` (Caddy + TLS, PostgreSQL, Redis, backup) | ✅ все сервисы healthy; HTTP→HTTPS; `/vk/callback` через Caddy отвечает строкой подтверждения |
| 9 | В контейнерном стеке: миграции и сид, CLI-приглашение, бэкап (`pg_dump` + файлы), загрузка фото в read-only контейнер (WebP, EXIF удалён), отказ поддельной «картинке» | ✅ |
| 10 | VK-бот: confirmation, отказ при неверном secret/group_id, дедупликация `event_id`, анкета в диалоге → заявка `VK_BOT` в БД | ✅ (отправка ответов в VK не проверялась — нет токена и сети) |
| 11 | Telegram-бот: обработчики на эмулированных апдейтах — привязка по одноразовому коду, отказ повторного кода, задачи и мероприятия, отказ командам командного состава для бойца | ✅ (реальный Bot API не проверялся — нет токена) |
| 12 | Воркер: доставка из outbox, очистка по срокам | ✅ |

## 2026-09-25 — Claude Code: режим ISPmanager, контент из методического пособия

| # | Действие | Результат |
|---|---|---|
| 1 | Разобран «Методическое пособие для конкурсных мероприятий» (.docx от владельца) | ✅ подтверждены командир и комиссар «Драго», список 10 ТОП Москвы, девизы, история и структура РСО |
| 2 | Публичный раздел «РСО и ТОП Москвы» (/about) и внутренняя «База знаний» в кабинете (РСО; памятка по трудоустройству подростков) | ✅ проверено в браузере; анонимный доступ к базе знаний → редирект на вход |
| 3 | Режим деплоя за nginx ISPmanager: `REVERSE_PROXY=ispmanager`, compose-оверлей без Caddy (порты только 127.0.0.1), `drago-proxy.conf` для `vhosts-resources`, установщик `install-nginx-proxy.sh` с `nginx -t` и откатом | ✅ проверено на nginx 1.30 с vhost в формате ISPmanager: статика, query, кодированные пути, POST, `/vk/callback`, `www`→apex, ACME/roundcube остаются за панелью, подмена XFF пресекается |
| 4 | Провайдер почты `MAIL_PROVIDER=ispmanager` (API панели: email.edit/suspend/resume/delete) | ✅ тесты с mock-сервером; пароли только в теле POST |
| 5 | Полный гайд по деплою `docs/deployment.md` | ✅ |

## Осталось сделать на сервере (владелец/администратор)

Порядок и команды — в [deployment.md](deployment.md) (сервер с ISPmanager и привязанным доменом):

- [ ] `00-server-audit.sh` — приложить отчёт в этот журнал (без секретов)
- [ ] `01-backup-existing.sh` — скачать архив с сервера
- [ ] `02-bootstrap.sh`
- [ ] DNS в REG.RU — [dns-records.md](dns-records.md)
- [ ] `.env` (`gen-secrets.sh` + SMTP, Telegram, VK) → `make deploy`
- [ ] `make invite-admin` → вход → 2FA
- [ ] SSH-ключи → `03-harden-ssh.sh`
- [ ] Боты: [bots.md](bots.md)
- [ ] Почта: выбрать вариант ([mail.md](mail.md)), пройти чек-лист проверки доставки
- [ ] Контент: чек-лист в [research-drago.md](research-drago.md)
- [ ] Внешний мониторинг `/api/health` и выгрузка бэкапов за пределы сервера
- [ ] Проверить физическое размещение VPS в РФ (152-ФЗ)
